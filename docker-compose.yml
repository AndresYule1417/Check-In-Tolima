services:
  web:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./app/data:/app/instance  # Para persistir la base de datos localmente
    restart: always
